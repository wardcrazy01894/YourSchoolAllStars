node_modules/
dist/
.env
.env.local
.DS_Store
*.log

# Transient git worktrees created for isolated subagent/reviewer runs.
.claude/worktrees/

# Adversarial-review output dropped in the repo root by reviewer agents.
.review-*.md

# Local-only harness settings.
.claude/settings.local.json

# Scratch dir created by data-pipeline scripts during local runs.
scripts/tmp/
data/_raw/

# UNC media-guide scans — large, and re-fetchable with data-work/unc/fetch-guides.mjs
# (145 MB of OCR XML + page images). The DERIVED files are committed: guide/,
# guide-resolved/, guide-rosters/ and the Vision OCR TSVs in guide-ocr/.
data-work/unc/guide-xml/
data-work/unc/guide-txt/
data-work/unc/guide-img/
