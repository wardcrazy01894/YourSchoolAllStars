dist
node_modules
package-lock.json

# Local-only / transient (not committed): harness settings + isolated worktrees.
.claude/settings.local.json
.claude/worktrees
.review-*.md

# Committed data-pipeline work-in-progress (big JSON drafts; PROGRESS.md is hand-formatted).
data-work
